# Ruby Code Invaders

A simple pattern matching system for detecting ASCII invaders from a radar sample.

## How to run

```bash
cd ruby-code-invaders

bundle install

# Add custom patterns to the assets/radar folder

ruby bin/code_invaders.rb sample1.txt
```

## How to test

```bash
cd ruby-code-invaders

bundle install

rspec
```
